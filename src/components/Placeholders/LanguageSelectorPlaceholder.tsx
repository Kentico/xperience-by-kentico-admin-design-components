import * as React from 'react';
import './LanguageSelectorPlaceholder.css'

/**
 * Placeholder component for language selector.
 * Serves as a mount point for language selector functionality.
 */
export const LanguageSelectorPlaceholder = () => {
  return (
    <div
      className={'LanguageSelectorPlaceholder'}
      id="languageSelectorPlaceholder"
    />
  )
}
