import * as React from 'react';
import './WorkspaceSelectorPlaceholder.css'

/**
 * Placeholder component for workspace selector.
 * Serves as a mount point for workspace selector functionality.
 */
export const WorkspaceSelectorPlaceholder = () => {
  return (
    <div
      className={'WorkspaceSelectorPlaceholder'}
      id="workspaceSelectorPlaceholder"
    />
  )
}
