import { type InfoCardData } from '@/components/InfoCard'

/**
 * Info card group properties.
 */
export interface InfoCardGroupProps {
  /**
   * Gets or sets an array of cards.
   */
  readonly cards: InfoCardData[]
}
